# Copilot 用リポジトリ指示（開発者向け）

このファイルは GitHub Copilot / 開発支援ツールがこのリポジトリで作業する際のガイドラインです。

## 主要ルール
- ソース内のコメントは**日本語**で記載してください（短い英語注釈は任意）。
- 関数名・ファイル名は既存の命名規則に合わせる（例: `efi_calc.c` / `efi_calc.h`）。
- 単体で変更可能な最小単位でコミットを作成してください。コミットメッセージは簡潔に英語／日本語で記載可。

## 新しい機能・モジュールを追加する場合
- 公開 API（他ファイルから呼ぶ関数）は必ずヘッダ（`Core/Inc`）に宣言を追加する。
- 実装は `Core/Src` に `.c` ファイルを置く。
- STM32CubeMX 生成の CMake 設定に従い、`cmake/stm32cubemx/CMakeLists.txt` の `MX_Application_Src` に新規ソースを追加すること。
  - 既存のパターンを参照してパスを追加してください。

## コメント・ドキュメント方針
- 関数の冒頭には目的・引数・戻り値（簡潔）を日本語で記載する。
- ハードウェア依存（GPIOピン、ADCチャネル、タイミング等）は明示する（例: `ADC_CHANNEL_6 を使用`）。
- TODO や FIXME は `/* TODO: ... */` 形式で残す。

## ビルドとテスト
- ローカルでのビルド手順（Windows PowerShell）:

```powershell
cmake --preset Debug
cmake --build build/Debug --config Debug
```

- ビルド前に `cmake` 設定が必要な場合は `CMakePresets.json` を確認する。

## MUX / ハードウェア制御について
- 外部アナログMUXなどを使用する場合は、GPIO の定義（`Core/Inc` のヘッダで `#define EFI_MUX_SEL0_GPIO_Port ...` など）をプロジェクトに追加してから実装する。
- 実装例では MUX 制御マクロが存在すれば使用する設計にしてください（存在しない場合は no-op）。

## コーディングスタイル（簡易）
- C 言語（C11 相当）で記述する。
- STM32 HAL API を使用する場合、HAL の戻り値をチェックしエラー処理を行う。
- 可能ならば副作用の少ない小関数に分割する。

## コミット & PR
- 大きな変更はブランチを切って PR を作成する。
- PR 説明には変更点、ビルド手順、動作確認手順を記載する。

## 連絡先／注記
- 自動生成コード（STM32CubeMX 由来）は極力そのまま残し、ユーザ編集領域（`/* USER CODE BEGIN */`）を利用してください。

---
以上。必要なら追加ルールや例を追記します。